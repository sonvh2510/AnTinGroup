@{
    Layout = null;
    // The ViewBag is used to enable the Breadcrumb on website
    // Default breadcrumb's showed if ViewBag.ShowBreadCrumb is null
    bool isShow = ViewBag.ShowBreadCrumb ?? true;
}

@if (isShow)
{
    @*
        Just put the static html is here
    *@
}